services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-springboot-graal-app:latest
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      # JAVA_TOOL_OPTIONS: "-Xmx512m"   # uncomment/tune as needed
    restart: unless-stopped
